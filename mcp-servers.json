{
  "mcpServers": {
    "serena": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "--network", "host",
        "-v", "D:\\tsumiki_sample:/workspaces/projects",
        "tsumiki_sample-serena-mcp",
        "python", "serena-mcp-server.py"
      ]
    }
  }
}

/*
注意事項:
1. 絶対パスをマウント: -v "D:\\tsumiki_sample:/workspaces/projects"
2. Windowsはバックスラッシュをエスケープ: D:\\path\\to\\project
3. STDIO運用: docker run -i で起動（composeで常駐しない）
4. イメージ名: tsumiki_sample-serena-mcp（docker compose buildで作成）
*/
